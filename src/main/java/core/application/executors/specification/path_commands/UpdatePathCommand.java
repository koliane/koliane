package core.application.executors.specification.path_commands;

import java.nio.file.Path;

public class UpdatePathCommand extends PathCommand{
    public UpdatePathCommand(Path path) {
        super(path);
    }
}
